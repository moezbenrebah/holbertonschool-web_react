Install Necessary Dependencies

Install axios-mock-adapter to mock API requests.

Ensure @testing-library/react, @testing-library/jest-dom, redux-mock-store, and other testing utilities are installed.

Set Up Test Environment

Configure testing setup to include Redux store mocking and API request mocking.

Import necessary modules like render, screen, userEvent, configureStore, etc.

⚾️ Test App Component

Test Display of Login Component

Create a mock store with isLoggedIn: false.

Render App with the mock store.

Verify that the Login component is displayed.

Test Display of CourseList Component

Create a mock store with isLoggedIn: true.

Render App with the mock store.

Verify that the CourseList component is displayed.

Test Fetching Notifications on Mount

Mock the fetchNotifications API using axios-mock-adapter.

Render App and check if fetchNotifications is called.

⚾️ Test Login Component

Test Input Fields Updates

Render Login.

Simulate input changes in email and password fields.

Verify input values update correctly.

Test Submit Button Disabled State

Render Login.

Verify submit button is disabled when inputs are empty.

Test Login Action Dispatch on Form Submission

Mock the login action from authSlice.

Simulate form submission with valid inputs.

Verify that the login action is dispatched with correct payload.

⚾️ Test Footer Component

Test Display of "Contact us" Link When Logged In

Create a mock store with isLoggedIn: true.

Render Footer with the mock store.

Verify that the "Contact us" link is displayed.

Test Hiding of "Contact us" Link When Logged Out

Create a mock store with isLoggedIn: false.

Render Footer with the mock store.

Verify that the "Contact us" link is not displayed.

⚾️ Test Header Component

Test Display of Logout Button and User Email When Logged In

Create a mock store with isLoggedIn: true and a user object.

Render Header with the mock store.

Verify that the logout button and user email are displayed.

Test Logout Action Dispatch on Logout Button Click

Mock the logout action from authSlice.

Simulate a click on the logout button.

Verify that the logout action is dispatched.

⚾️ Test Notifications Component

Test Display of Notifications When Drawer is Open

Create a mock store with displayDrawer: true and notifications.

Render Notifications with the mock store.

Verify that the notifications are displayed.

Test Mark Notification as Read

Mock the markNotificationAsRead action.

Simulate a click on a notification.

Verify that the markNotificationAsRead action is dispatched with the correct id.

Test Toggle Drawer Visibility

Simulate clicks on "Your notifications" text and the close button.

Verify that the showDrawer and hideDrawer actions are dispatched.

⚾️ Test CourseList Component

Test Display of "No course available yet" Message

Create a mock store with an empty courses array.

Render CourseList with the mock store.

Verify that the message is displayed.

Test Display of List of Courses

Create a mock store with a list of courses.

Render CourseList with the mock store.

Verify that the courses are displayed in the table.

⚾️ Test NotificationItem Component

Test Rendering of Default Notification

Render NotificationItem with type="default" and a value.

Verify the component renders with correct text, data attribute, and style.

Test Rendering of Urgent Notification with HTML

Render NotificationItem with type="urgent" and an html prop.

Verify the component renders with correct data attribute, style, and dangerouslySetInnerHTML.

Test Rendering of Urgent Notification Without HTML

Render NotificationItem with type="urgent" and a value.

Verify the component renders with correct text, data attribute, and style.

⚾️ Test CourseListRow Component

Test Rendering of Header Row with One Cell

Render CourseListRow with isHeader={true} and textFirstCell.

Verify it renders a header row with one cell spanning two columns.

Test Rendering of Header Row with Two Cells

Render CourseListRow with isHeader={true}, textFirstCell, and textSecondCell.

Verify it renders a header row with two cells.

Test Rendering of Regular Row

Render CourseListRow with isHeader={false}, textFirstCell, and textSecondCell.

Verify it renders a regular row with two cells.

⚾️ Test BodySection Component

Test Rendering of Title and Children

Render BodySection with a title and children.

Verify it renders the title and children correctly.

⚾️ Test BodySectionWithMarginBottom Component

Test Rendering with Margin Class

Render BodySectionWithMarginBottom with a title and children.

Verify it renders the BodySection with the correct margin class and includes title and children.

Test Instructions
For Each Component:

Create separate test files (e.g., App.test.js, Login.test.js, etc.).

Use render from @testing-library/react to render components.

Use userEvent for simulating user interactions.

Use expect and custom matchers from @testing-library/jest-dom for assertions.

Mock Redux store using configureStore from @reduxjs/toolkit and provideMockStore from redux-mock-store.

Mock API calls using axios-mock-adapter where necessary.

Specific Instructions:

App Component:

Use render with a preloaded state for isLoggedIn.

Use waitFor or async functions to handle side effects like API calls.

Login Component:

Use getByLabelText or getByPlaceholderText to target input fields.

Use fireEvent.change to simulate input changes.

Use getStore to capture dispatched actions.

Footer Component:

Use render with different preloaded states for isLoggedIn.

Use queryByRole to check for the presence or absence of links.

Header Component:

Render with a preloaded state containing isLoggedIn: true and a user object.

Use fireEvent.click to simulate logout button click.

Notifications Component:

Mock showDrawer, hideDrawer, and markNotificationAsRead actions.

Use fireEvent.click to simulate user interactions.

CourseList Component:

Mock the courses state with empty and populated arrays.

Use getByRole with row and cell roles to verify table structure.

NotificationItem Component:

Test different types and props (e.g., type="default", type="urgent", html).

Use toHaveStyle and toHaveAttribute for style and attribute checks.

CourseListRow Component:

Test different props combinations (isHeader, textFirstCell, textSecondCell).

Verify the correct HTML structure using getByRole.

BodySection Component:

Render with a title and children components.

Verify the presence and content of <h2> and children.

BodySectionWithMarginBottom Component:

Render with a title and children.

Verify the margin class is applied and content is rendered correctly.

General Tips:

Ensure all tests are isolated and do not affect each other.

Use beforeEach to set up common mocks or store configurations.

Keep tests readable and well-organized, grouping related tests together.


⭐️ faulty components:
🧩️ App:
import { useSelector, useDispatch } from 'react-redux';
import { fetchNotifications } from './features/notifications/notificationsSlice';
import { fetchCourses } from './features/courses/coursesSlice';
import Header from './components/Header/Header';
import Footer from './components/Footer/Footer';
import Login from './pages/Login/Login';
import CourseList from './pages/CourseList/CourseList';
import Notifications from './components/Notifications/Notifications';
import BodySection from './components/BodySection/BodySection';
import BodySectionWithMarginBottom from './components/BodySectionWithMarginBottom/BodySectionWithMarginBottom';

export default function App() {
  const dispatch = useDispatch();
  const isLoggedIn = useSelector((state) => state.auth.isLoggedIn);

  useEffect(() => {
    dispatch(fetchNotifications());
  }, [dispatch]);

  useEffect(() => {
    if (isLoggedIn) {
      dispatch(fetchCourses());
    }
  }, [dispatch, isLoggedIn]);

  return (
    <>
      <Notifications />
      <Header />
      {/* Intentional fault: Always display Login */}
      <BodySectionWithMarginBottom title="Log in to continue">
        <Login />
      </BodySectionWithMarginBottom>
      <BodySection title="News from the School">
        <p>Holberton School news goes here</p>
      </BodySection>
      <Footer />
    </>
  );
}

🧩️ Login:
import { useDispatch } from 'react-redux';
import WithLogging from '../../components/HOC/WithLogging';
import useLogin from '../../hooks/useLogin';
import './Login.css';
import { login } from '../../features/auth/authSlice';

function Login() {
  const dispatch = useDispatch();
  const {
    email,
    password,
    enableSubmit,
    // Intentional fault: Removed onChange handlers
    handleChangeEmail,
    handleChangePassword,
    handleLoginSubmit
  } = useLogin({
    onLogin: (email, password) => dispatch(login({ email, password }))
  });

  return (
    <form aria-label="form" onSubmit={handleLoginSubmit}>
      <div className="App-body">
        <p>Login to access the full dashboard</p>
        <div className="form">
          <label htmlFor="email">Email</label>
          <input
            type="email"
            name="email"
            id="email"
            value={email}
            // Intentional fault: Removed onChange
          />
          <label htmlFor="password">Password</label>
          <input
            type="password"
            name="password"
            id="password"
            value={password}
            // Intentional fault: Removed onChange
          />
          <input
            type="submit"
            value="OK"
            disabled={!enableSubmit}
          />
        </div>
      </div>
    </form>
  );
}

🧩️ Footer:
import { useSelector } from 'react-redux';
import { getCurrentYear, getFooterCopy } from '../../utils/utils';
import './Footer.css';

export default function Footer() {
  const { isLoggedIn } = useSelector((state) => state.auth);

  return (
    <div className="App-footer">
      <p>Copyright {getCurrentYear()} - {getFooterCopy(true)}</p>
      {/* Intentional fault: Always display "Contact us" */}
      <a href="#">Contact us</a>
    </div>
  );
}

🧩️ Header:
import { useSelector, useDispatch } from 'react-redux';
import { logout } from '../../features/auth/authSlice';
import './Header.css';
import logo from '../../assets/holberton-logo.jpg';

export default function Header() {
  const dispatch = useDispatch();
  const { isLoggedIn, user } = useSelector((state) => state.auth);

  const handleLogout = () => {
    dispatch(logout());
  };

  return (
    <div className="App-header">
      <img src={logo} className="App-logo" alt="holberton logo" />
      <h1>School Dashboard</h1>
      {/* Intentional fault: Removed condition for isLoggedIn */}
      <div id="logoutSection">
        Welcome <b>{user.email}</b> <a href="#" onClick={handleLogout}>(logout)</a>
      </div>
    </div>
  );
}

🧩️ Notifications:
import { memo, useCallback } from 'react';
import { useSelector, useDispatch } from 'react-redux';
import { showDrawer, hideDrawer } from '../../features/notifications/notificationsSlice';
import NotificationItem from '../NotificationItem/NotificationItem';
import './Notifications.css';
import closeIcon from '../../assets/close-icon.png';

const Notifications = memo(function Notifications() {
  const dispatch = useDispatch();
  const { notifications, displayDrawer } = useSelector((state) => state.notifications);

  const handleDisplayDrawer = useCallback(() => {
    dispatch(showDrawer());
  }, [dispatch]);

  const handleHideDrawer = useCallback(() => {
    dispatch(hideDrawer());
  }, [dispatch]);

  // Intentional fault: Removed markNotificationAsRead dispatch
  return (
    <>
      <div className="notification-title" onClick={handleDisplayDrawer}>
        Your notifications
      </div>
      {displayDrawer && (
        <div className="Notifications">
          {notifications.length > 0 ? (
            <>
              <p>Here is the list of notifications</p>
              <button onClick={handleHideDrawer} aria-label="Close">
                <img src={closeIcon} alt="close icon" />
              </button>
              <ul>
                {notifications.map((notification) => (
                  <NotificationItem
                    key={notification.id}
                    id={notification.id}
                    type={notification.type}
                    value={notification.value}
                    html={notification.html}
                    // Intentional fault: Removed markAsRead prop
                  />
                ))}
              </ul>
            </>
          ) : (
            <p>No new notifications for now</p>
          )}
        </div>
      )}
    </>
  );
});

🧩️ CourseList:
import CourseListRow from './CourseListRow/CourseListRow';
import { useSelector } from 'react-redux';
import './CourseList.css'
import WithLogging from '../../components/HOC/WithLogging';

function CourseList() {
  const { courses } = useSelector((state) => state.courses);

  return (
    <div className="courses">
      {courses.length > 0 ? (
        <table id="CourseList">
          <thead>
            <CourseListRow textFirstCell="Available courses" isHeader={true} />
            <CourseListRow textFirstCell="Course name" textSecondCell="Credit" isHeader={true} />
          </thead>
          <tbody>
            {courses.map((course) => (
              <CourseListRow key={course.id} textFirstCell={course.name} textSecondCell={course.credit} />
            ))}
          </tbody>
        </table>
      ) : (
        // Intentional fault: Removed the message
        <table id="CourseList">
          <thead>
            {/* Intentional fault: Empty thead */}
          </thead>
        </table>
      )}
    </div>
  );
}

🧩️ NotificationItem:
import { memo } from 'react';

const NotificationItem = memo(function NotificationItem({ 
  type,
  html,
  value,
  markAsRead,
  id
}) {
  if (type === 'default') {
    return (
      <li
        style={{ color: "green" }} // Intentional fault: Changed color to green
        data-notification-type={type}
        onClick={() => markAsRead(id)}
      >
        {value}
      </li>
    );
  }
  
  if (type === 'urgent' && html !== undefined) {
    return (
      <li
        style={{ color: "red" }}
        data-notification-type={type}
        dangerouslySetInnerHTML={html}
        onClick={() => markAsRead(id)}
      />
    );
  }
  
  return (
    <li
      style={{ color: "red" }}
      data-notification-type={type}
      onClick={() => markAsRead(id)}
    >
      {value}
    </li>
  );
});

🧩️ CourseListRow
export default function CourseListRow({ 
  isHeader = false, 
  textFirstCell = '', 
  textSecondCell = null 
}) {
  return (
    isHeader ? (
      <tr>
        <th colSpan={2}> // Intentional fault: Incorrect colSpan
          {textFirstCell}
        </th>
        {textSecondCell ? <th>{textSecondCell}</th> : null}
      </tr>
    ) : (
      <tr>
        <td>{textFirstCell}</td>
        <td>{textSecondCell}</td>
      </tr>
    )
  )
}

🧩️ BodySection:
export default function BodySection({ title, children }) {
  return (
    <div className='bodySection'>
      <h2>{title}</h2>
      {/* Intentional fault: Removed children */}
    </div>
  )
}

🧩️ BodySectionWithMarginBottom:
import BodySection from '../BodySection/BodySection';
import './BodySectionWithMarginBottom.css';

export default function BodySectionWithMarginBottom({ title, children }) {
  return (
    <div> // Intentional fault: Removed className
      <BodySection title={title}>
        {children}
      </BodySection>
    </div>
  )
}

































